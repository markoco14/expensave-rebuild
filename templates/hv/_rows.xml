<items xmlns="https://hyperview.org/hyperview">
    <item key="progress" style="progress-track">
        <view style="progress-bar">{% if percent_remaining >= 50 %}<text style="text-dark text-large">${{daily_spending_bucket.daily_amount - total_spent}}</text>{% endif %}</view>
        {% if percent_remaining <= 50 %}
        <text style="text-dark text-large">${{daily_spending_bucket.daily_amount - total_spent}}</text>
        {% endif %}
    </item>
    <item key="buckets">
        <text style="text-dark text-large label">Add purchase in:</text>
        <view scroll="true" scroll-orientation="horizontal" shows-scroll-indicator="false">
            <view style="bucket-bar">
                {% for bucket in buckets %}
                <view style="bucket-bar-item">
                    <behavior trigger="press" href="/hv/today/new?bucket={{ bucket.bucket_id }}" action="new"/>
                    <text style="text-dark text-large">{{ bucket.name }}</text>
                </view>
                {% endfor %}
            </view>
        </view>
    </item>
    <item>
        <view style="total">
            <text style="total-text text-dark text-large">Total spent:</text>
            <text style="total-text text-dark text-large">${{ total_spent }}</text>
        </view>
    </item>
    {% for purchase in purchases %}
    <item key="{{ loop.index0 }}" style="list-item">
        <behavior action="push" href="/hv/purchases/{{ purchase.purchase_id }}"/>
        <text style="list-item-text text-large text-dark">{{ purchase.purchased_at.strftime("%H:%M") }}</text>
        <text style="list-item-text text-large text-dark">${{ purchase.amount }}</text>
    </item>
    {% endfor %}
</items>