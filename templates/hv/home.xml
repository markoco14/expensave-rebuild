<doc xmlns="https://hyperview.org/hyperview">
    <screen>
        <styles>
            <style id="container" paddingLeft="8" paddingRight="8"/>
        </styles>
        <body>
            <view style="container">
                <text>{{ today_date.strftime("%B %d, %Y") }}</text>
                {% if not daily_spending_bucket %}
                    <text>You haven't set up your daily budget for this month.</text>
                    <text>Click here to set your budget</text>
                {% else %}
                    <text>Money remaining: ${{ daily_spending_bucket.daily_amount - total_spent }} / ${{ daily_spending_bucket.daily_amount }}</text>
                    <text>Add purchase</text>
                    {% if purchases %}
                        <list>
                            {% for purchase in purchases %}
                                <item key="{{ loop.index0 }}">
                                    <text>${{ purchase.amount }} in {{ purchase.bucket_name }}</text>
                                </item>
                            {% endfor %}
                        </list>
                    {% endif %}
                {% endif %}
            </view>
        </body>
  </screen>
</doc>